# Taste
- Do the implementation, not just an analysis; keep changes small and verifiable, and never jump ahead into speculative later phases (new SDKs/integrations) while the current milestone is unfinished. Confidence: 0.9
- Before reporting completion, inspect the final diff and run real verification (build, install, interactive/screenshots on emulator), then report honestly what was and wasn't verified. Confidence: 0.85
- Never commit, push, or deploy unless the user explicitly asks. Confidence: 0.95
- Never claim verification on hardware that wasn't actually tested; keep emulator evidence and physical-device gates clearly separate. Confidence: 0.9
- Close bug fixes with behavioral evidence (force-stop/relaunch, re-run the scenario), not just storage or log inspection; remove temporary debug logging once the diagnosis is proven. Confidence: 0.85
- Preserve existing working behavior and the user's uncommitted work while making changes; never revert unrelated changes or staged deletions in a shared live workspace. Confidence: 0.9
- Android/Gradle builds use Android Studio's bundled JBR (`JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"`) with `ANDROID_HOME="$HOME/Library/Android/sdk"`; standard verification is `./gradlew assembleDebug` plus `git diff --check`. Confidence: 0.9
- Encrypted/binary design assets (e.g. `Launcher UI Kit.pen`) must be accessed only through their dedicated MCP tooling (Pencil MCP) — never `cat`, `grep`, `rg`, Python, or any filesystem parsing of the file itself. Confidence: 0.95
- Test launcher work on the API 28 arm64 emulator (`emulator-5554`) via `adb install -r` plus screenshots; the connected Samsung SM_A156E is explicitly not target hardware. Confidence: 0.85
- API 28 / Android 9 compatibility is a hard requirement for the Car Launcher project (YT5760D/AC8257, arm64-v8a, 1600×720, ~160 dpi). Confidence: 0.9
- Don't hard-code UI to one resolution; keep layouts adaptive to the design system's widescreen approach with proper API 28 touch targets. Confidence: 0.85
- Respect clean-rewrite boundaries: when a codebase is intentionally rebuilt from scratch, don't resurrect the old deleted architecture (e.g. the oversized old ViewModel/widget architecture). Confidence: 0.85
- Use the design source of truth (UI Kit) for all visuals — spacing, sizes, surfaces, typography, touch behavior — and treat external reference apps (Mini AA) as structure/placement inspiration only, never as a visual reference. Confidence: 0.9
- Prefer owned in-app entries/tiles over fake installed packages when adding first-party destinations (e.g. Settings inside the app drawer). Confidence: 0.8
- When combining suspended loads with state flows, always copy into the latest state after the suspension completes — a stale `uiState.copy(...)` can silently overwrite emissions (e.g. DataStore pins) that arrived mid-suspension. Confidence: 0.65
- The user also works directly on macOS (shell tasks involve /Applications, keychain, codesign, Gatekeeper). Confidence: 0.8
- User's home directory is `/Users/arnab`; their default SSH key lives at `/Users/arnab/.ssh/id_ed25519` (verified on disk). Confidence: 0.9
- For local macOS code signing, use the valid keychain identity "SmartTyper Local Debug" (found via `security find-identity -v -p codesigning`) rather than ad-hoc signing. Confidence: 0.7
- When a macOS app bundle has a revoked/broken signature (e.g. Gatekeeper `CSSMERR_TP_CERT_REVOKED`): diagnose with `codesign -dv`/`spctl -a` first, then sign inside-out (deepest Mach-Os → nested runtime/framework bundles → dylibs → outer bundle) with `codesign --force --sign`, strip `com.apple.quarantine`, verify with `codesign --verify --deep --strict`, and launch the app to confirm it stays running. Confidence: 0.7
- Also administers remote Linux VPS servers as root over SSH (provisioning tasks like appending to `authorized_keys`, managing `.ssh` perms) and treats them as direct-execute ops, not advisory questions. Confidence: 0.6
- For one-off server tasks, supplies root passwords inline and expects them used directly (e.g. via `sshpass`) rather than refused or converted into interactive password prompts. Confidence: 0.55
